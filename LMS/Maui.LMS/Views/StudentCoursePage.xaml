<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.LMS.Views.StudentCoursePage"
             Title="Student Courses"
             NavigatedTo="StudentCoursePageNavigatedTo">

    <Grid
        Padding="30"
        RowDefinitions="Auto,Auto,*,Auto"
        RowSpacing="20">

        <Label
            Grid.Row="0"
            Text="Student Courses"
            FontSize="32"
            HorizontalOptions="Center" />

        <Label
            x:Name="StudentNameLabel"
            Grid.Row="1"
            FontSize="22"
            HorizontalOptions="Center" />

        <CollectionView
            x:Name="CoursesCollectionView"
            Grid.Row="2"
            SelectionMode="Single"
            SelectionChanged="CourseSelectionChanged">

            <CollectionView.EmptyView>
                <Label
                    Text="This student is not enrolled in any courses."
                    FontSize="17"
                    HorizontalOptions="Center"
                    VerticalOptions="Center" />
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <VerticalStackLayout
                        Padding="12"
                        Spacing="4">

                        <Label
                            Text="{Binding Name}"
                            FontSize="20"
                            FontAttributes="Bold" />

                        <Label
                            Text="{Binding Code,
                                   StringFormat='Course Code: {0}'}" />

                        <Label
                            Text="{Binding Semester,
                                   StringFormat='Semester: {0}'}" />

                        <Label
                            Text="{Binding Section,
                                   StringFormat='Section: {0}'}" />

                        <Label
                            Text="{Binding Description}" />

                    </VerticalStackLayout>

                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

        <HorizontalStackLayout
            Grid.Row="3"
            Spacing="15"
            HorizontalOptions="Center">

            <Button
                Text="Open Selected Course"
                Clicked="OpenCourseClicked" />

            <Button
                Text="Select Different Student"
                Clicked="SelectDifferentStudentClicked" />

        </HorizontalStackLayout>

    </Grid>

</ContentPage>